<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GravityJelly">

        <!-- AdMob App ID — TEST id khi dev (technical §Services). Thay id thật khi phát hành. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <!-- Play Games Services — PROJECT ID (số) do Play Console sinh ra. Hiện là PLACEHOLDER:
             thay bằng id thật để bật Bảng xếp hạng toàn cầu. PlayGamesManager tự nhận biết
             placeholder (LEADERBOARD_ID) → không init SDK, chạy bảng nội bộ, KHÔNG crash. -->
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="@string/game_services_project_id" />

        <!-- Đa ngôn ngữ (per-app language): AppCompatDelegate.setApplicationLocales tự LƯU và
             KHÔI PHỤC lựa chọn ngôn ngữ qua service này (autoStoreLocales) trên mọi API ≥ 24. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden|locale|layoutDirection"
            android:theme="@style/Theme.GravityJelly.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
