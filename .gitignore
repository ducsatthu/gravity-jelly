*.iml
.gradle
/local.properties
/.idea/*
!/.idea/icon.png
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/*/build

# Kotlin compiler session files
.kotlin/

# Khoá ký release — KHÔNG BAO GIỜ commit (giữ .example làm mẫu)
keystore.properties
*.jks
*.keystore

# Secrets môi trường (token deploy Vercel…) — KHÔNG commit (giữ .env.example)
.env
.env.*
!.env.example

# IDE (Eclipse/BuildShip) compiled output
bin/
core/bin/
